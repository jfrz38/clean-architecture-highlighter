local SqlUserRepository = require("infrastructure.persistence.sql_user_repository")
local CreateUser = require("application.use_cases.create_user")
local UserId = require("domain.user_id")

local User = {}

return User
